#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

void strcopy(char* src, char** dst) {
    int len = 0;
    char *ptr = src;

    while(*ptr != '\0') {
        len++;
        ptr++;
    }



}
